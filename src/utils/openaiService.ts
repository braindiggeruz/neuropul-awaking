export interface OpenAIResponse {
  success: boolean;
  content: string;
  error?: string;
}

export interface MemeImageResponse {
  success: boolean;
  imageUrl?: string;
  error?: string;
}

// Проверка доступности API ключа
// FIXED: Removed hardcoded API key and use environment variable instead
const API_KEY = import.meta.env.VITE_OPENAI_API_KEY;

export async function askOpenAI(prompt: string): Promise<OpenAIResponse> {
  try {
    console.log('[OpenAI] Making request with prompt:', prompt.substring(0, 100) + '...');
    
    // Валидация входных данных
    if (!prompt || prompt.trim().length === 0) {
      return {
        success: false,
        content: '',
        error: 'Пустой промт'
      };
    }

    // Ограничение длины промта
    if (prompt.length > 2000) {
      return {
        success: false,
        content: '',
        error: 'Промт слишком длинный (максимум 2000 символов)'
      };
    }

    // FIXED: Use local API endpoint instead of direct OpenAI call
    const response = await fetch("/api/gpt", {
      method: "POST",
      headers: {
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        prompt: prompt,
        systemPrompt: "Ты - AI-наставник NeuropulAI. Отвечай кратко, полезно и вдохновляюще. Используй эмодзи. Максимум 500 слов. Будь практичным и конкретным. Обучай пользователя лучшим практикам работы с ИИ."
      })
    });

    console.log('[OpenAI] Response status:', response.status);

    if (!response.ok) {
      const errorData = await response.json().catch(() => ({}));
      console.error('[OpenAI] API Error:', errorData);
      
      if (response.status === 401) {
        return {
          success: false,
          content: '',
          error: 'Неверный API ключ OpenAI'
        };
      } else if (response.status === 429) {
        return {
          success: false,
          content: '',
          error: 'Превышен лимит запросов. Попробуй через минуту ⏰'
        };
      } else if (response.status === 500) {
        return {
          success: false,
          content: '',
          error: 'Сервер OpenAI временно недоступен 🔧'
        };
      } else {
        return {
          success: false,
          content: '',
          error: `Ошибка API: ${response.status}`
        };
      }
    }

    const content = await response.json();
    console.log('[OpenAI] Response data received');
    
    if (!content || (typeof content === 'string' && content.trim().length === 0)) {
      return {
        success: false,
        content: '',
        error: 'AI вернул пустой ответ'
      };
    }

    console.log('[OpenAI] Success! Content length:', typeof content === 'string' ? content.length : 'object');
    return {
      success: true,
      content: typeof content === 'string' ? content.trim() : content,
      error: undefined
    };

  } catch (error) {
    console.error('[OpenAI] Request failed:', error);
    
    if (error instanceof TypeError && error.message.includes('fetch')) {
      return {
        success: false,
        content: '',
        error: 'Проблема с сетью. Проверь интернет-соединение 📡'
      };
    }
    
    return {
      success: false,
      content: '',
      error: 'Неизвестная ошибка. Попробуй позже 🔄'
    };
  }
}

export async function generateMemeImage(prompt: string): Promise<MemeImageResponse> {
  try {
    console.log('[DALL-E] Generating enhanced meme for prompt:', prompt);
    
    // Валидация входных данных
    if (!prompt || prompt.trim().length === 0) {
      return {
        success: false,
        error: 'Пустая тема для мема'
      };
    }

    // Ограничение длины промта
    if (prompt.length > 300) {
      return {
        success: false,
        error: 'Тема слишком длинная (максимум 300 символов)'
      };
    }

    // FIXED: Use memegen.link instead of DALL-E to avoid API calls
    // This is a simple, free API that doesn't require authentication
    const words = prompt.trim().split(' ');
    const midPoint = Math.ceil(words.length / 2);
    const topText = words.slice(0, midPoint).join(' ');
    const bottomText = words.slice(midPoint).join(' ') || 'AI мощь!';
    
    const encodedTop = encodeURIComponent(topText);
    const encodedBottom = encodeURIComponent(bottomText);
    const imageUrl = `https://api.memegen.link/images/success/${encodedTop}/${encodedBottom}.webp`;

    console.log('[MEME] Generated URL:', imageUrl);
    
    return {
      success: true,
      imageUrl: imageUrl
    };
  } catch (error) {
    console.error('[MEME] Request failed:', error);
    
    if (error instanceof TypeError && error.message.includes('fetch')) {
      return {
        success: false,
        error: 'Проблема с сетью. Проверь интернет-соединение 📡'
      };
    }
    
    return {
      success: false,
      error: 'Неизвестная ошибка при генерации мема 🔄'
    };
  }
}

// Улучшение промпта с помощью ИИ
export async function improvePrompt(originalPrompt: string, toolId: string): Promise<OpenAIResponse> {
  try {
    const improvementPrompt = `Улучши этот промпт для ${toolId === 'meme-generator' ? 'создания мема' : 'AI-инструмента'}: "${originalPrompt}"

Сделай промпт более:
- Детальным и конкретным
- Эмоционально окрашенным
- Визуально описательным (если нужно изображение)
- Профессиональным

${toolId === 'meme-generator' ? 
  'Для мемов добавь: стиль, настроение, композицию, цветовую гамму.' :
  'Для текстовых инструментов добавь контекст и желаемый формат ответа.'}

Верни только улучшенный промпт без объяснений.`;

    return await askOpenAI(improvementPrompt);
  } catch (error) {
    console.error('Error improving prompt:', error);
    return {
      success: false,
      content: '',
      error: 'Ошибка улучшения промпта'
    };
  }
}

// Получение подсказок для промптов
export const getPromptSuggestions = (toolId: string): string[] => {
  const suggestions = {
    'meme-generator': [
      'Кот-программист за компьютером в 3 утра, уставший но решительный, неоновое освещение',
      'Собака в костюме бизнесмена на важной встрече, серьезное выражение, офисная обстановка',
      'Панда ест бамбук и смотрит Netflix, расслабленная атмосфера, уютная комната',
      'Робот пытается понять человеческие эмоции, футуристический стиль, яркие цвета',
      'Пингвин в солнечных очках на пляже, летняя атмосфера, тропический фон'
    ],
    'image-analyzer': [
      'Проанализируй композицию этого изображения и дай советы по улучшению',
      'Определи настроение и эмоциональное воздействие этой фотографии',
      'Опиши цветовую палитру и её влияние на восприятие изображения'
    ],
    'idea-generator': [
      'Создай 5 идей для заработка на AI в сфере образования',
      'Придумай бизнес-идеи для фрилансеров с использованием ChatGPT',
      'Идеи для AI-стартапа в области здоровья и фитнеса'
    ],
    'code-assistant': [
      'Создай функцию для валидации email на JavaScript с подробными комментариями',
      'Оптимизируй этот SQL-запрос для лучшей производительности',
      'Напиши React-компонент для загрузки файлов с прогресс-баром'
    ],
    'mvp-generator': [
      'AI-помощник для планирования путешествий с персонализацией',
      'Платформа для обучения детей программированию через игры',
      'Сервис для автоматического создания резюме на основе LinkedIn'
    ]
  };

  return suggestions[toolId as keyof typeof suggestions] || [];
};

// Специализированные промты для каждого инструмента с обучающими элементами
export const getToolPrompt = (toolId: string, userInput: string = ''): string => {
  switch (toolId) {
    case 'idea-generator':
      return `Ты - эксперт по AI-бизнесу и монетизации. Создай 5 оригинальных и реалистичных идей для заработка с помощью AI в 2025 году на основе запроса: "${userInput}". 

ВАЖНО: Каждая идея должна быть выполнимой для новичков, но прибыльной.

Каждая идея должна включать:
- 💡 **Название идеи** (креативное и запоминающееся)
- 📝 **Описание** (2-3 предложения с конкретными деталями)
- 💰 **Примерный доход** (реалистичная оценка в месяц)
- 🚀 **Первые 3 шага** (конкретные действия для старта)
- ⏱️ **Время до результата** (реалистичные сроки)
- 🎯 **Целевая аудитория** (кто будет платить)
- ⚠️ **Подводные камни** (что может пойти не так)

Формат ответа:
**💡 Идея 1: [Креативное название]**
📝 [Детальное описание с примерами]
💰 Доход: [сумма] в месяц
🎯 Аудитория: [кто платит]
🚀 Шаги: 
   1) [конкретный шаг с деталями]
   2) [конкретный шаг с деталями] 
   3) [конкретный шаг с деталями]
⏱️ Результат через: [время]
⚠️ Риски: [что учесть]

🧠 **Обучающий совет**: Объясни, почему эти идеи работают в 2025 году и какие AI-инструменты использовать.`;

    case 'image-analyzer':
      return `Ты - эксперт по визуальному анализу и дизайну. Проанализируй следующее изображение или ситуацию: "${userInput}". 

Дай профессиональный анализ:

🔍 **Визуальный анализ:**
[Детальное описание того, что видно]

🎨 **Композиция и дизайн:**
- Правило третей: [как используется]
- Цветовая палитра: [анализ цветов и их воздействия]
- Освещение: [источники света, тени, настроение]
- Баланс элементов: [как распределены объекты]

🧠 **Психологическое воздействие:**
- Эмоции: [какие чувства вызывает]
- Настроение: [общая атмосфера]
- Символизм: [скрытые смыслы]

💡 **Практические рекомендации:**
- Что улучшить: [конкретные советы]
- Альтернативные подходы: [другие варианты]
- Техники для усиления эффекта: [профессиональные приемы]

🎯 **Применение:**
[Где и как можно использовать такой подход]

🧙‍♂️ **Обучающий момент**: Объясни принципы хорошего визуального дизайна на этом примере.`;

    case 'code-assistant':
      return `Ты - senior-разработчик и ментор. Проанализируй код или техническую задачу: "${userInput}".

Дай экспертную помощь:

🔍 **Анализ кода:**
[Что делает этот код, архитектура, подход]

⚠️ **Найденные проблемы:**
- 🐛 Баги: [критические ошибки]
- 🚨 Уязвимости: [проблемы безопасности]  
- 📈 Производительность: [узкие места]
- 🧹 Чистота кода: [нарушения best practices]

✅ **Улучшенная версия:**
\`\`\`javascript
// Комментарии объясняющие улучшения
[Исправленный и оптимизированный код]
\`\`\`

💡 **Объяснение улучшений:**
[Почему эти изменения лучше, какие принципы применены]

🎯 **Best Practices:**
- [Принцип 1 с примером]
- [Принцип 2 с примером]
- [Принцип 3 с примером]

🧪 **Тестирование:**
[Как протестировать этот код]

🚀 **Следующие шаги:**
[Что изучить дальше для роста]

🧙‍♂️ **Обучающий момент**: Объясни ключевые концепции программирования на этом примере.`;

    case 'meme-generator':
      return `Ты - креативный директор и эксперт по вирусному контенту. Создай концепт для мема на тему: "${userInput}".

🎨 **Визуальный концепт:**
[Детальное описание сцены, персонажей, обстановки]

📝 **Текстовые элементы:**
- Верхний текст: [если нужен]
- Основная шутка: [главный текст мема]
- Дополнительные элементы: [подписи, стикеры]

😂 **Анатомия юмора:**
[Почему это смешно, какой механизм юмора используется]

🎭 **Эмоциональный профиль:**
- Настроение: [какие эмоции вызывает]
- Целевая аудитория: [кому понравится]
- Вирусный потенциал: [почему будут делиться]

🔥 **Альтернативные варианты:**
1. [Вариант 1 с другим подходом]
2. [Вариант 2 с другим стилем]
3. [Вариант 3 с другой подачей]

🎯 **Технические детали для DALL-E:**
- Стиль: [конкретный художественный стиль]
- Цветовая гамма: [какие цвета использовать]
- Композиция: [как расположить элементы]
- Качество: [HD, 4K, профессиональный]

🧙‍♂️ **Обучающий момент**: Объясни принципы создания вирусного контента и психологию мемов.`;

    case 'mvp-generator':
      return `Ты - product manager и стартап-консультант. Создай детальный план MVP для идеи: "${userInput}".

🎯 **Продуктовое видение:**
- Проблема: [какую боль решаем]
- Решение: [как решаем в одном предложении]
- Уникальность: [чем отличаемся от конкурентов]

👥 **Целевая аудитория:**
- Основная: [кто главный пользователь]
- Вторичная: [кто еще может использовать]
- Персоны: [2-3 детальных портрета пользователей]

⚡ **Core Features MVP (3-5 функций):**
1. [Функция 1] - [зачем нужна, как работает]
2. [Функция 2] - [зачем нужна, как работает]
3. [Функция 3] - [зачем нужна, как работает]

🛠️ **Технический стек:**
- Frontend: [технологии с обоснованием]
- Backend: [технологии с обоснованием]
- База данных: [решение с обоснованием]
- Хостинг: [платформа с обоснованием]
- AI/ML: [какие AI-сервисы использовать]

📅 **Roadmap (12 недель):**
Недели 1-3: [конкретные задачи и результаты]
Недели 4-6: [конкретные задачи и результаты]
Недели 7-9: [конкретные задачи и результаты]
Недели 10-12: [конкретные задачи и результаты]

💰 **Бизнес-модель:**
- Монетизация: [как зарабатываем]
- Ценообразование: [сколько стоит]
- Unit Economics: [экономика на одного пользователя]

📊 **Ключевые метрики:**
- Acquisition: [как измеряем привлечение]
- Activation: [как измеряем активацию]
- Retention: [как измеряем удержание]
- Revenue: [как измеряем доход]

🚀 **Go-to-Market:**
1. [Канал привлечения 1]
2. [Канал привлечения 2]
3. [Канал привлечения 3]

⚠️ **Риски и митигация:**
- [Риск 1]: [как снизить]
- [Риск 2]: [как снизить]
- [Риск 3]: [как снизить]

🧙‍♂️ **Обучающий момент**: Объясни принципы Lean Startup и почему важно начинать с MVP.`;

    default:
      return `Ты - AI-наставник и эксперт. Помоги с задачей: "${userInput}". 

Дай структурированный и обучающий ответ:

🎯 **Понимание задачи:** 
[Анализ проблемы и контекста]

💡 **Решение:** 
[Пошаговый план действий]

🚀 **Практическая реализация:** 
[Конкретные инструменты и методы]

⚠️ **Важные моменты:** 
[Что обязательно учесть]

🎓 **Дополнительное обучение:**
[Что изучить для углубления знаний]

🧙‍♂️ **Обучающий момент**: Объясни принципы и лучшие практики в этой области.

Будь максимально практичным, конкретным и обучающим.`;
  }
};

// Функция для получения заголовка результата
export const getToolResultTitle = (toolId: string): string => {
  switch (toolId) {
    case 'idea-generator':
      return '💡 AI-эксперт создал бизнес-идеи:';
    case 'image-analyzer':
      return '🔍 Профессиональный визуальный анализ:';
    case 'code-assistant':
      return '💻 Senior-разработчик отвечает:';
    case 'meme-generator':
      return '🎨 Концепт вирусного мема готов:';
    case 'mvp-generator':
      return '🚀 Детальный MVP-план создан:';
    default:
      return '🤖 AI-наставник отвечает:';
  }
};